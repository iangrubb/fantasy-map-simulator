
const initialState = {
    mapData: []
}

const mapReducer = (state = initialState.mapData, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default mapReducer